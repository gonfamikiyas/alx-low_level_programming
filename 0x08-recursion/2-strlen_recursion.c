#include "main.h"
#include <string.h>
#include <stdio.h>
/**
*_strlen_recursion - returns the len of a string
*@s:char
*@i - int
*Return: Always 0.
*/
int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
